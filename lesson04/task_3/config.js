'use strict';

/**
 * Список идентификаторов загадок, можно заменить на любые другие, н-р: [1, 2, 3, 4...], [а, б, в...]...
 * @type {{identify_list: string[]}}
 */
const config = {
    identify_list: ['a', 'b', 'c', 'd', 'e', 'f', 'g'],
};

